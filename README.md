# student_subject_manager

In order to start this Project,
Run the Following Command:

In Root File: yarn run server
in Client file : yarn satrt

Before running the above command,Please insatll all dependencies.
